org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

android.useAndroidX=true
android.nonTransitiveRClass=true

kotlin.code.style=official

# Published version of the four Android modules. One value: they ship together
# and depend on each other, so misaligned versions would produce combinations
# nobody ever tested.
appwinSdkVersion=0.1.0
